/**
 * Family-facing translations, keyed by the Portuguese source string (the PT
 * copy in the code is the key). Missing entries fall back to Portuguese.
 * Professional surfaces (pediatrician/back-office) are intentionally PT-only.
 */
export const TR: Record<'en' | 'es', Record<string, string>> = {
  en: {},
  es: {},
};
